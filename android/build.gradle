// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        buildToolsVersion = "33.0.0"
        minSdkVersion = 21
        compileSdkVersion = 33
        targetSdkVersion = 33

        // We use NDK 23 which has both M1 support and is the side-by-side NDK version from AGP.
        ndkVersion = "23.1.7779620"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath('com.google.firebase:firebase-bom:32.3.1')
        // Other Firebase dependency possibilities:
        // classpath('com.google.firebase:firebase-analytics')
        // classpath('com.google.firebase:firebase-messaging')
        // classpath('com.google.firebase:firebase-crashlytics')
        // classpath('com.google.firebase:firebase-perf')
        // classpath('com.google.firebase:firebase-appindexing')
        // classpath('com.google.firebase:firebase-auth')
        // classpath('com.google.firebase:firebase-firestore')
        // classpath('com.google.firebase:firebase-storage')
        // classpath('com.google.firebase:firebase-database')
        // classpath('com.google.firebase:firebase-functions')
        // classpath('com.google.firebase:firebase-config')
        // classpath('com.google.firebase:firebase-inappmessaging-display')
        // classpath('com.google.firebase:firebase-inappmessaging')
        // classpath('com.google.firebase:firebase-remote-config')
        // classpath('com.google.firebase:firebase-dynamic-links')
        // classpath('com.google.firebase:firebase-abt')
        // classpath('com.google.firebase:firebase-appcheck')
        // classpath('com.google.firebase:firebase-ml-vision')
        // classpath('com.google.firebase:firebase-ml-model-interpreter')
        // classpath('com.google.firebase:firebase-ml-natural-language')
        // classpath('com.google.firebase:firebase-ml-natural-language-language-id')
        // classpath('com.google.firebase:firebase-ml-natural-language-smart-reply')
        // classpath('com.google.firebase:firebase-ml-natural-language-translate')
        // classpath('com.google.firebase:firebase-ml-natural-language-translation-model')
        // classpath('com.google.firebase:firebase-ml-model-interpreter')

        classpath("com.google.gms:google-services:4.3.3")
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
    }
}
